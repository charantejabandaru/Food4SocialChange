const donationStatus = {
    raised : "raised",
    accepted : "accepted",
    pickedup : "pickedup",
    delivered: "delivered"
}

const volunteerStatus = {
    available : "available",
    assigned : "assigned"
}

module.exports = {donationStatus : donationStatus ,
                  volunteerStatus : volunteerStatus 
                };
